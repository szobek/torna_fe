

export class StorageEdit {
   public getServerkeyFromStorage(): string {
        let serverKey: string | null
        //console.log(window)
        serverKey =""
        if (typeof serverKey === "string")
            return serverKey
        else
            return ""
    }
}